.vscode/*
.DS_Store
node_modules/

# Playwright run artifacts (baselines in scripts/visual-regression/baselines ARE committed)
test-results/
playwright-report/

# Local, machine-specific deploy target for `npm run deploy` (never commit).
.env
# CodeBuddy local project data (never commit)
.codebuddy/
