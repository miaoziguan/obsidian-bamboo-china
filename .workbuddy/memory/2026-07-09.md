# 2026-07-09

## 卡片圆角精细化调节功能
- 在 Cupertino 主题 Style Settings 增加「卡片圆角」滑块（`id: radius-modifier`）。
- 实现方式：暴露主题既有的 `--radius-modifier` 倍数变量（主题内 28 处 `calc(Xpx * var(--radius-modifier))` 引用），滑块范围 0–2、步长 0.05、默认 1。0=直角，1=默认，2=更圆润。统一影响卡片/标注/代码块/弹窗圆角。
- 修改文件：`src/app/style-settings.scss` 与 `theme.css` 的 `@settings` 块（均插入在 `transparent-sidebar` 之后），并 `cp` 同步到测试仓库 `CJ/obsidian-vault/.obsidian/themes/Cupertino/theme.css`。
- 顺手修复：`@settings` 块内无障碍分组那批滑块（之前会话加的，含 bold-modifier/line-height-normal 等）用了 tab 缩进，严格 js-yaml 会拒收。已将两块文件 `@settings` 区内的 tab 统一转成 4 空格，js-yaml 校验通过（共 40 条目）。

## 验证方式
- 用 js-yaml（Style Settings 同款解析器）校验 `@settings` 块：PARSE OK，radius-modifier 条目合法。
